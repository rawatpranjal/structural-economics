#!/bin/sh
ignored=$(git ls-files -ci --exclude-standard)
if [ -n "$ignored" ]; then
  echo "ERROR: these tracked files match .gitignore patterns (untrack with git rm --cached, or fix .gitignore):"
  echo "$ignored"
  exit 1
fi
