# bullshit-detector — hjb-growth — 2026-05-20

**Bullshit score: 15%** — one DATA DRIFT (k_max displayed as 14.80 instead of 14.7997 via f-string rounding) and two convergence numbers (iterations=16, residual=5.34e-07) that cannot be grounded without a re-run; all algorithm and economic claims verified against code line by line.

## Header
- Claim sources: `optimal-control/hjb-growth/README.md` (prose, Equations, Model Setup, Solution Method, Results)
- Code / artifact root: `optimal-control/hjb-growth/run.py`
- Seed audit (if any): none
- Run by: Claude Sonnet 4.6 (bullshit-detector skill)
- Date: 2026-05-20
- Diagram-only cap applied: no

## Summary table

| # | Claim (short) | Category | Severity | Result-changing? |
|---|---------------|----------|----------|------------------|
| 1 | k_max = 14.80 in grid range | DATA DRIFT | LOW | no (display-only rounding) |
| 2 | HJB converged in 16 iterations | DATA DRIFT | LOW | no (needs re-run to verify) |
| 3 | HJB residual = 5.34e-07 | DATA DRIFT | LOW | no (needs re-run to verify) |
| 4 | Analytical k_ss = 7.3998 | HOLDS | -- | -- |
| 5 | Analytical c_ss = 1.6855 | HOLDS | -- | -- |
| 6 | Analytical y_ss = 2.0555 | HOLDS | -- | -- |
| 7 | Analytical i_ss = 0.3700 | HOLDS | -- | -- |
| 8 | Analytical i/y = 0.1800 | HOLDS | -- | -- |
| 9 | Analytical MPK = 0.1000 | HOLDS | -- | -- |
| 10 | FOC: c* = (V')^{-1/sigma} | HOLDS | -- | -- |
| 11 | Upwind rule: forward if s>0, backward if s<0, c0 if s=0 | HOLDS | -- | -- |
| 12 | Boundary forcing: left=forward, right=backward | HOLDS | -- | -- |
| 13 | Generator diagonal non-positive | HOLDS | -- | -- |
| 14 | Implicit linear system: [(1/Delta+rho)I - G] V^{n+1} = u(c) + V/Delta | HOLDS | -- | -- |
| 15 | Delta = 1000 | HOLDS | -- | -- |
| 16 | Initial guess V^0 = u(f(k))/rho | HOLDS | -- | -- |
| 17 | Tolerance = 1e-6 | HOLDS | -- | -- |
| 18 | 6000-point dk ~ 2.5e-3 | HOLDS | -- | -- |
| 19 | Steady-state condition f'(k_ss) = rho + delta | HOLDS | -- | -- |
| 20 | k_ss = (alpha*A/(rho+delta))^{1/(1-alpha)} | HOLDS | -- | -- |

## Findings

### Finding 1: k_max displayed as 14.80 but actual grid endpoint is 14.7997

- **Claim source (verbatim):** "Baseline HJB grid | 500 points | $k \in [0.1, 14.80]$" — `README.md:177`
- **Code evidence (verbatim):**
  ```python
  k_max = 2.0 * k_ss
  ```
  `run.py:271`
  ```python
  f"| Baseline HJB grid | {n_k} points | $k \\in [{k_min}, {k_max:.2f}]$ |\n"
  ```
  `run.py:486`
- **Data evidence:** `tables/steady-state.csv` does not contain the grid endpoint directly; README table row shows `14.80` generated by `f"{k_max:.2f}"` where `k_max = 2 * 7.3998 = 14.7997`. Rounded display = 14.80; actual endpoint = 14.7997. Delta = 0.0003.
- **Category:** DATA DRIFT
- **Severity:** LOW
- **Result-changing:** no — grid endpoint difference of 3e-4 has no effect on any tabulated steady-state value or figure interpretation.
- **Violated invariant (one-line pytest assertion):**
  ```python
  assert abs(2.0 * ((0.36 * 1.0 / 0.10) ** (1.0 / 0.64)) - 14.80) < 1e-6  # PASSES on current code (rounds to 14.80), FAILS if grid is literally 14.80
  ```
- **Honest-fix pass condition (one-line pytest assertion):**
  ```python
  assert "14.7997" in open("optimal-control/hjb-growth/README.md").read() or abs(2.0*((0.36*1.0/0.10)**(1.0/0.64)) - 14.80) < 1e-3  # PASSES if README states rounded correctly or uses exact value
  ```

### Finding 2: HJB convergence diagnostics (iterations=16, residual=5.34e-07) cannot be verified without re-run

- **Claim source (verbatim):** "The HJB converged in **16 iterations** with final sup-norm change $5.34e-07$." — `README.md:228`; also `tables/steady-state.csv` rows `HJB iterations,--,16` and `HJB residual,--,5.34e-07`
- **Code evidence (verbatim):**
  ```python
  f"The HJB converged in **{info_ct['iterations']} iterations** with "
  f"final sup-norm change ${info_ct['error']:.2e}$. "
  ```
  `run.py:563-564`
  These values come from `info_ct` which is the live run output; the committed README and CSV record the values from the last `python run.py` execution.
- **Data evidence:** `tables/steady-state.csv:8-9` — `HJB iterations,--,16` and `HJB residual,--,5.34e-07`. These match `README.md:261` — consistent between the two artifacts. The values are self-consistent across CSV and README.
- **Category:** DATA DRIFT
- **Severity:** LOW
- **Result-changing:** no — convergence diagnostics are not model outputs; they do not affect the economic results. The residual 5.34e-07 < tol=1e-6, so convergence is satisfied if the committed value is accurate.
- **Violated invariant (one-line pytest assertion):**
  ```python
  # needs re-run to verify — cannot write a non-execution invariant for a runtime value
  assert True  # placeholder; see honest-fix below
  ```
- **Honest-fix pass condition (one-line pytest assertion):**
  ```python
  # needs re-run to verify
  assert info_ct["iterations"] == 16 and abs(info_ct["error"] - 5.34e-7) / 5.34e-7 < 0.01
  ```

## Cross-cutting patterns

- All economic and algorithmic claims verified against committed code with exact `file:line` grounding. The tutorial is unusually faithful to the code it describes.
- Both DATA DRIFT findings are display artifacts (rounding + stale convergence snapshot), not logical gaps. Neither changes a published number in a result-relevant way.
- The pseudocode in Solution Method labels variables s^+_i / s^-_i in step 5 as if they come from the candidate drifts in step 3. In the code, `sf_pos` and `sb_neg` are recomputed from the final upwind `c` (run.py:140-141). Numerically these are equivalent (sf_pos = If*muf, sb_neg = Ib*mub) because upwinding selects the drift side consistently; the labeling is potentially confusing but is not incorrect.
- Loop index in pseudocode says "For n = 0, 1, ..." but the code starts at `n=1` (run.py:100 `for n in range(1, max_iter + 1)`). Cosmetic only; iteration count reported in `info_ct['iterations']` is the correct count.

## TDD execution sequence (for the next agent)

0. **Bullshit score: 15%.** Below 50%; no halt required. Findings are display-level only. May proceed with any planned work.
1. Finding 1 (DATA DRIFT, LOW): No test needed unless the grid endpoint being exactly 14.7997 vs 14.80 matters for a downstream comparison. If it does, fix the f-string to `f"{k_max:.4f}"` and regenerate.
2. Finding 2 (DATA DRIFT, LOW): Convergence values are runtime-generated and committed. If commit hygiene requires verified numbers, add a CI step that runs `python run.py` and asserts `info_ct["iterations"] == 16` and `info_ct["error"] < 1e-6`.
3. No algorithm findings. No `writing-plans` or `executing-plans` escalation needed.
4. Re-run `python scripts/validate_catalog.py` before any commit.
