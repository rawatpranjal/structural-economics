# bullshit-detector — metropolis-hastings — 2026-05-20

**Bullshit score: 15%** — one DATA DRIFT finding (LOW severity): the Model Setup table row "MH draws | 20,000 | After burn-in of 1,000" implies 20,000 retained draws, but the code produces 19,000 retained draws (20,000 total minus 1,000 burned); all other claims hold against code and data artifacts.

## Header
- Claim sources: `computational-methods/metropolis-hastings/README.md` (Overview, Equations, Model Setup, Solution Method, Results)
- Code / artifact root: `computational-methods/metropolis-hastings/run.py`
- Data artifacts: `computational-methods/metropolis-hastings/tables/conjugate-summary.csv`, `computational-methods/metropolis-hastings/tables/proposal-comparison.csv`
- Seed audit (if any): None
- Run by: bullshit-detector agent (Claude Sonnet 4.6), 2026-05-20
- Diagram-only cap applied: no

## Summary table

| # | Claim (short) | Category | Severity | Result-changing? |
|---|---------------|----------|----------|------------------|
| 1 | "MH draws | 20,000 | After burn-in of 1,000" implies 20,000 retained | DATA DRIFT | LOW | no (diagnostic only; Results correctly report 19,000) |
| 2 | Beta-Binomial conjugate update (alpha_post, beta_post, mean, variance) | HOLDS | — | — |
| 3 | Posterior mean = convex combo of prior mean and sample fraction | HOLDS | — | — |
| 4 | Tail probability via regularized incomplete Beta | HOLDS | — | — |
| 5 | Worked example Beta(4,2) mean = 0.667 | HOLDS | — | — |
| 6 | log_mvn formula matches Equations bivariate Gaussian | HOLDS | — | — |
| 7 | MH acceptance ratio = min(1, kernel ratio), log form | HOLDS | — | — |
| 8 | 1D bounded chain rejects outside (0,1) | HOLDS | — | — |
| 9 | ESS truncation at first nonpositive lag | HOLDS | — | — |
| 10 | Mixture TRUE_MEAN = (0,0) and TRUE_COV diagonal = 3.25 | HOLDS | — | — |
| 11 | Results table numbers match tables/*.csv exactly | HOLDS | — | — |
| 12 | Acceptance 69.9% for step 0.6 consistent with CSV | HOLDS | — | — |

## Findings

### Finding 1: Model Setup "MH draws | 20,000 | After burn-in of 1,000" implies 20,000 retained draws

- **Claim source (verbatim):** `"| MH draws | 20,000 | After burn-in of 1,000 |"` — `README.md:167`
- **Code evidence (verbatim):**
  ```python
  n_conj_draws = 20_000
  burn_conj = 1_000
  proposal_conj = 0.10
  bb_draws, bb_acceptance = random_walk_mh_1d(
      beta_binomial_log_posterior,
      n_draws=n_conj_draws,
      ...
  )
  bb_kept = bb_draws[burn_conj:]
  ```
  `run.py:225-236`
- **Data evidence:** The figure caption reads `"MH histogram (19,000 draws)"` (generated by `run.py:531`). The Solution Method prose (generated by `run.py:481`) reads `"After 1,000 burn-in draws and 19,000 retained draws"`.
- **Category:** DATA DRIFT — the Model Setup table row says "MH draws | 20,000 | After burn-in of 1,000", which a reader will parse as 20,000 retained plus 1,000 burned = 21,000 total. The code runs 20,000 total with 1,000 burned, yielding 19,000 retained. The Solution Method prose and the figure caption correctly state 19,000. The table row and the prose describe the same run but give inconsistent implied totals.
- **Severity:** LOW — the correct count (19,000) appears in two other locations in the same README; no published number is wrong.
- **Result-changing:** no — the Results section reports the correct empirical values derived from the 19,000 retained draws. The table row wording is a labeling ambiguity, not a computational error.
- **Violated invariant (one-line pytest assertion):**
  ```python
  assert "19,000" in open("computational-methods/metropolis-hastings/README.md").read() and "After burn-in of 1,000" in open("computational-methods/metropolis-hastings/README.md").read() and "20,000 | After burn-in of 1,000" in open("computational-methods/metropolis-hastings/README.md").read()
  # PASSES on current code (all three strings present, internally inconsistent); FAILS once table row is fixed
  ```
- **Honest-fix pass condition (one-line pytest assertion):**
  ```python
  assert "19,000 | After burn-in of 1,000" in open("computational-methods/metropolis-hastings/README.md").read() or "20,000 total" in open("computational-methods/metropolis-hastings/README.md").read()
  # PASSES on honest fix (table row disambiguated); FAILS on current code
  ```

## Cross-cutting patterns

- No systematic mislabeling or parameter leakage. All algorithm implementations (conjugate update, 1D bounded MH, 2D mixture MH, ESS, mode_labels) match their prose descriptions exactly.
- The single DATA DRIFT finding (Model Setup table vs Solution Method prose) is a column-header ambiguity pattern: the "MH draws" row label does not clarify whether the count is total or retained. If a second tutorial in this catalog uses the same Model Setup table format and the same row label, inspect whether the count there is total or retained to avoid the same drift.
- All numeric results in `tables/conjugate-summary.csv` and `tables/proposal-comparison.csv` are self-consistent with the README tables and with the formulas in code. No stale-run data drift.

## TDD execution sequence (for the next agent)

0. **Bullshit score is 15% (< 50%).** No halt required. Safe to proceed.
1. Turn the **violated invariant** into a pytest test confirming the current README contains the ambiguous "20,000 | After burn-in of 1,000" pattern. Confirm the test passes on the current file (proves the ambiguity is real).
2. Turn the **honest-fix pass condition** into a second pytest test that fails on the current file. The pair is the red/green spec.
3. Fix the Model Setup table row in `run.py` (the table is generated from the f-string at `run.py:449`). Change `f'| MH draws | {n_conj_draws:,} | After burn-in of {burn_conj:,} |'` to disambiguate, e.g. `f'| MH draws (total) | {n_conj_draws:,} | Retained: {n_conj_draws - burn_conj:,} after burn-in of {burn_conj:,} |'` or split into two rows.
4. Regenerate `README.md` by running `python run.py` inside the tutorial folder.
5. Re-run this skill on the new README to confirm Finding 1 now reads HOLDS and the new bullshit score is 0-10%.
