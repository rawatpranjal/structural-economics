# bullshit-detector — bayesian-optimization — 2026-05-20

**Bullshit score: 30%** — one FALSE claim (two orders of magnitude for a 33.6x ratio) and one MISLABELED claim (zero-mean GP in Equations while code implements constant-mean GP); neither changes any published number, but a hostile reviewer catches both immediately from the table.

## Header
- Claim sources: `numerical-methods/bayesian-optimization/README.md` (Equations, Results, Takeaway sections)
- Code / artifact root: `numerical-methods/bayesian-optimization/run.py`
- Data artifacts: `numerical-methods/bayesian-optimization/tables/method_comparison.csv`, `numerical-methods/bayesian-optimization/tables/bo_iteration_log.csv`
- Seed audit (if any): none
- Run by: bullshit-detector agent (Claude Sonnet 4.6)
- Date: 2026-05-20
- Diagram-only cap applied: no

## Summary table

| # | Claim (short) | Category | Severity | Result-changing? |
|---|---------------|----------|----------|------------------|
| 1 | "We use a zero-mean prior, f ~ GP(0, k)" with formula using raw y | MISLABELED | MED | no |
| 2 | "two orders of magnitude smaller than simulated annealing" | FALSE | MED | no |
| 3 | "one order smaller than random search" | DILUTED | LOW | no |
| 4 | All Model Setup parameters | HOLDS | - | - |
| 5 | All per-iteration log numbers | HOLDS | - | - |
| 6 | Method comparison table numbers (BO=30, MS=312, RS=500, SA=1007) | HOLDS | - | - |
| 7 | Evaluations to global (BO=12, MS=5, RS=140, SA=6) | HOLDS | - | - |
| 8 | EI closed-form formula and worked example arithmetic | HOLDS | - | - |
| 9 | Peak values (p_L^*=1.6029, pi=4.1360, p_H^*=4.25, pi=5.625) | HOLDS | - | - |
| 10 | GP posterior variance formula | HOLDS | - | - |
| 11 | Pseudocode in Solution Method (shows y-mean(y) correctly) | HOLDS | - | - |

## Findings

### Finding 1: "We use a zero-mean prior" — code uses constant-mean GP

- **Claim source (verbatim):** "We use a zero-mean prior, $f \sim \mathcal{GP}(0, k)$" and posterior formula "$\mu(x_{\ast}) = k(x_{\ast}, X) \left[K(X,X) + \sigma_n^2 I\right]^{-1} y$" — `README.md:38` and `README.md:49`
- **Code evidence (verbatim):**
  ```python
  def fit(self, X: np.ndarray, y: np.ndarray) -> "GaussianProcess":
      self.X = np.atleast_1d(X).astype(float)
      self.y = np.atleast_1d(y).astype(float)
      self.y_mean = float(self.y.mean())
      y_centered = self.y - self.y_mean
      K = rbf_kernel(self.X, self.X, self.length_scale, self.sigma_f)
      K += (self.sigma_n ** 2) * np.eye(len(self.X))
      self._cho = cho_factor(K, lower=True)
      self._alpha = cho_solve(self._cho, y_centered)
      return self

  def predict(self, X_star: np.ndarray) -> tuple[np.ndarray, np.ndarray]:
      X_star = np.atleast_1d(X_star).astype(float)
      K_s = rbf_kernel(self.X, X_star, self.length_scale, self.sigma_f)
      mu = self.y_mean + K_s.T @ self._alpha
  ```
  `run.py:68-86`
- **Data evidence:** Not applicable — this is a formula claim, not a numeric claim in the results tables.
- **Category:** MISLABELED
- **Severity:** MED
- **Result-changing:** no — the tables are generated by the code, which correctly implements the constant-mean GP. The formula in `Equations` does not change any published number; it teaches the reader the wrong equation.
- **Additional note:** The pseudocode in `Solution Method` (`README.md:122-125`) correctly writes `a = solve(L^T, solve(L, y - mean(y)))` and `mu = mean(y) + k_s^T @ a`, which matches the code. The `Equations` section mathematical display is therefore internally inconsistent with the `Solution Method` pseudocode in the same README. A zero-mean GP would use `a = K^{-1} y` (no subtraction) and `mu = k_s^T @ a` (no offset). The code adds `self.y_mean` (the sample mean of training targets, not zero) as a constant-mean term.
- **Violated invariant (one-line pytest assertion):**
  ```python
  assert gp.y_mean == 0.0  # PASSES on current code if claim were true; FAILS because y_mean = mean(y) != 0
  ```
- **Honest-fix pass condition (one-line pytest assertion):**
  ```python
  assert "zero-mean" not in readme_equations_section or "mean(y)" in readme_equations_formula
  ```

### Finding 2: "Two orders of magnitude smaller than simulated annealing"

- **Claim source (verbatim):** "The Bayesian-optimization budget is two orders of magnitude smaller than simulated annealing and one order smaller than random search or multi-start." — `README.md:180`
- **Code evidence (verbatim):**
  ```python
  report.add_results(
      "The comparison table is normalized on the same objective and bracket. "
      "All four methods recover the global peak. "
      "The Bayesian-optimization budget is two orders of magnitude smaller than simulated annealing "
      "and one order smaller than random search or multi-start."
  )
  ```
  `run.py:635-638`
- **Data evidence:** `tables/method_comparison.csv` row values: BO `Function evaluations = 30`, SA `Function evaluations = 1007`. Ratio = 1007/30 = 33.6x = log10(33.6) = 1.53 orders of magnitude. Two orders of magnitude = 100x. The actual ratio is 33.6x, not 100x.
- **Category:** FALSE
- **Severity:** MED
- **Result-changing:** no — the raw numbers (30 vs 1007) are correct in the table and the description in the preceding prose paragraph. Only the "two orders of magnitude" label attached to those numbers is wrong. A reader who looks at the table rather than the prose label gets the correct picture.
- **Violated invariant (one-line pytest assertion):**
  ```python
  assert math.log10(1007 / 30) >= 2.0  # PASSES on current claim; FAILS on data (1.53 < 2.0)
  ```
- **Honest-fix pass condition (one-line pytest assertion):**
  ```python
  assert 1.4 <= math.log10(1007 / 30) <= 1.7  # PASSES on data (1.53); description must say "~1.5 orders" or ">30x"
  ```

### Finding 3: "One order smaller than random search"

- **Claim source (verbatim):** "one order smaller than random search or multi-start" — `README.md:180`
- **Code evidence (verbatim):** Same `run.py:637-638` as Finding 2.
- **Data evidence:** `tables/method_comparison.csv`: BO = 30, RS = 500. Ratio = 500/30 = 16.7x = log10(16.7) = 1.22 orders. One order of magnitude = 10x. The ratio is 16.7x, closer to 1.2 orders than 1.0 order. Multi-start = 312: ratio = 312/30 = 10.4x = 1.02 orders (approximately one order; borderline HOLDS).
- **Category:** DILUTED
- **Severity:** LOW
- **Result-changing:** no — the raw numbers remain in the table.
- **Violated invariant (one-line pytest assertion):**
  ```python
  assert math.log10(500 / 30) <= 1.05  # PASSES on "approximately one order" claim; FAILS on data (1.22)
  ```
- **Honest-fix pass condition (one-line pytest assertion):**
  ```python
  assert math.log10(500 / 30) > 1.1  # PASSES on data; description should say ">10x" not "one order"
  ```

## Cross-cutting patterns

- Both imprecise claims (Findings 2 and 3) appear in the same sentence in `README.md:180` and are hardcoded verbatim in `run.py:637-638`. The prose was written before the run numbers were fixed, and the "orders of magnitude" label was never reconciled with the actual table values. Any future run that changes SA or RS evaluation counts will preserve the wrong label silently.
- The zero-mean vs constant-mean inconsistency (Finding 1) is a split-document problem: `Equations` teaches the zero-mean formula, `Solution Method` pseudocode teaches the constant-mean formula. Both appear in the same generated README. A reader who only reads `Equations` learns the wrong equation; a reader who reads both sections sees a contradiction.
- All numeric results in the tables (`method_comparison.csv`, `bo_iteration_log.csv`) match the README prose and are internally consistent. The two false claims are both in descriptive prose labels, not in the data themselves.

## TDD execution sequence (for the next agent)

0. **Bullshit score is 30%.** Below 50% threshold; surface to user as informational, not a halt. No sibling artifact re-audit needed.
1. For Finding 1: write a test that instantiates `GaussianProcess`, fits it on non-zero-mean data, and asserts `gp.y_mean != 0.0`. This test PASSES on current code (proves the mean is non-zero, contradicting the "zero-mean" label).
2. For Finding 2: write a test that reads `tables/method_comparison.csv`, extracts BO and SA evaluation counts, and asserts `math.log10(sa_evals / bo_evals) < 2.0`. This test PASSES on current data (1.53 < 2.0).
3. For Finding 3: write a test that reads the same CSV and asserts `math.log10(rs_evals / bo_evals) > 1.1`. This test PASSES on current data (1.22 > 1.1).
4. Fixes: (a) Change `Equations` section to describe a constant-mean prior or change the code to actually use zero mean (remove `y_mean`). (b) Change "two orders of magnitude" to "roughly 30x" or "1.5 orders" in both `README.md:180` and `run.py:637`. (c) Change "one order smaller than random search" to ">10x smaller than random search" or similar.
5. After fixes, re-run `python run.py` to regenerate README. Re-run this skill to confirm score drops to 0-10%.
